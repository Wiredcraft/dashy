angular.module('Dashboard.Utils', [])

.factory('sortDate', function($rootScope) {
    // todo 
    // .filter('',  function() {
    // return function (data) {
    //     var parseDate = d3.time.format("%d-%b-%y").parse;

    //     angular.forEach(data, function(data, index, source) {
    //         var target = data.value
    //         target.date = parseDate(target.date);
    //     });
        
    //     data.sort(function(a, b) { return a['value']['date'] - b['value']['date'] });

    //     return data;
    // }
    // })
});