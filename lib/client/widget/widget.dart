library widget;

/**
 * Contains information on the layout of the widget and which type it is
 * Its type is inferred by the model type it has.
 *
 */
class Widget {
  var model;
  Widget(this.model);
}
